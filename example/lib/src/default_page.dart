import 'package:flutter/material.dart';

class DeaultPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) => const Scaffold(
        body: Center(
          child: Text('route is not exist!'),
        ),
      );
}
