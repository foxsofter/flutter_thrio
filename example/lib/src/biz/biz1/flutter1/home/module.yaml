type:
  - Goodman: "package:gooadman/ggodman.dart"
context:
  declaration:
    - stringKeyBiz1:

  definition:
    - stringKeyBiz1:
        type: String
        default: weewr
        method:
          get:
          getOrNull:
          getOrDefault:
          set:
          remove:
          on:
          onNull:
          onInitial:
    - intKeyRootModule:
        type: int
        method:
          get:
          set:
          on:
          onNull:
          onInitial:
    - people:
        type: People
        method:
          get:
          getOrNull:
          getOrDefault:
          set:
          remove:
          on:
          onNull:
          onInitial:


notify:

push:
  doc: 测试 集合 传参
  parameter:
    - strList:
        type: List<String>
        default: const <String>[]
        doc: "hello, this is a list."
    - goodMap:
        type: Map<String, dynamic>
        default: const <String, dynamic>{}
        doc: "hello, this is a map."
